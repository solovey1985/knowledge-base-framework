{
  "name": "{{PROJECT_NAME}}",
  "version": "{{VERSION}}",
  "description": "{{DESCRIPTION}}",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "node build.js",
    "build:watch": "nodemon build.js"
  },
  "dependencies": {
    "@solovey1985/knowledge-base-framework": "{{FRAMEWORK_VERSION}}",
    "express": "{{EXPRESS_VERSION}}"
  },
  "devDependencies": {
    "nodemon": "{{NODEMON_VERSION}}"
  }
}